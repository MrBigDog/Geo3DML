SET(TARGET_H    )
SET(TARGET_SRC dllTest.cpp)

INCLUDE_DIRECTORIES(${GWEARTH_SOURCE_DIR}/Src ${GWEARTH_SOURCE_DIR}/Src/vtklibxml2 ${ICONV_INCLUDE_DIR} )

SET(TARGET_LIBRARIES_VARS ICONV_LIBRARIES)
	
#LINK_INTERNAL(${LIB_NAME} gwEarth gwUtil)
SET(TARGET_COMMON_LIBRARIES  GMXMLFileIO GMLFeature GM_XML vtklibxml2 vtkExtending)
	
SET(EXECUTABLE_OUTPUT_PATH ${OUTPUT_LIBDIR})
SETUP_APPLICATION(dllTest)